name: commons_entities
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dependencies:
    path: ../dependencies
dev_dependencies:
  build_runner: ^2.3.0
  flutter_lints: ^2.0.1
  graphql_codegen: ^0.13.5
  freezed: ">=1.1.1 < 2.3.2"
  json_serializable: ^6.5.4
  hive_generator: ^2.0.0

  # test
  flutter_test:
    sdk: flutter
flutter:

